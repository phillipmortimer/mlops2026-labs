[project]
name = "lab-02-llmops"
version = "0.1.0"
description = "PDPSL lab scaffold for Langfuse-based LLMOps"
requires-python = ">=3.11,<3.14"
dependencies = [
  "langfuse==4.3.1",
  "openai==2.32.0",
  "python-dotenv==1.2.1",
]

[tool.uv]
package = false
